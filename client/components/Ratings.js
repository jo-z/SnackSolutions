import React from "react";
import { connect } from "react-redux";

export class Ratings extends React.Component {
	render() {
		return <p>hello!</p>;
	}
}

export default connect(null, null)(Ratings);
